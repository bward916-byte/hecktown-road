
/* =====================================================================
   THE BACK FORTY (M24) — beyond the lot: drop yard, shed, food truck, dog park, siding, canal
   ===================================================================== */
const BACK_W = 3000;
function drawTrailerParked(g, x, gy, num) { g.fillStyle = 'rgba(0,0,0,.18)'; g.beginPath(); g.ellipse(x + 100, gy, 120, 6, 0, 0, Math.PI * 2); g.fill(); const gr = g.createLinearGradient(0, gy - 110, 0, gy - 30); gr.addColorStop(0, '#eceae4'); gr.addColorStop(1, '#c6c2b8'); g.fillStyle = gr; rr(g, x, gy - 110, 200, 80, 4); g.fill(); g.strokeStyle = 'rgba(0,0,0,.08)'; for (let k = x + 8; k < x + 200; k += 8) { g.beginPath(); g.moveTo(k, gy - 108); g.lineTo(k, gy - 32); g.stroke(); } g.fillStyle = '#2c3038'; g.fillRect(x + 10, gy - 30, 180, 6); g.fillRect(x + 150, gy - 24, 6, 24); g.fillStyle = '#1b1d22'; g.beginPath(); g.arc(x + 44, gy - 12, 12, 0, Math.PI * 2); g.arc(x + 70, gy - 12, 12, 0, Math.PI * 2); g.fill(); g.fillStyle = '#243447'; g.font = 'bold 14px Georgia, serif'; g.textAlign = 'center'; g.fillText('PHILLIPS', x + 100, gy - 66); g.fillStyle = '#f2b544'; g.fillRect(x + 50, gy - 60, 100, 2); g.fillStyle = '#5b6670'; g.font = 'bold 9px monospace'; g.fillText('T-' + num, x + 100, gy - 40); g.textAlign = 'left'; }
function drawBoxcar(g, x, gy, col, mark) { g.fillStyle = '#5a5a60'; g.fillRect(x - 40, gy - 8, 320, 4); g.fillRect(x - 40, gy - 2, 320, 3); g.fillStyle = col; rr(g, x, gy - 96, 240, 78, 4); g.fill(); g.fillStyle = 'rgba(0,0,0,.25)'; g.fillRect(x + 100, gy - 90, 44, 66); g.fillStyle = '#1b1d22'; for (const wx of [x + 40, x + 66, x + 174, x + 200]) { g.beginPath(); g.arc(wx, gy - 10, 10, 0, Math.PI * 2); g.fill(); } g.fillStyle = 'rgba(255,255,255,.8)'; g.font = 'bold 10px monospace'; g.textAlign = 'left'; g.fillText(mark, x + 12, gy - 80); }
function drawShed(g, x, gy) { g.fillStyle = '#7a8088'; g.fillRect(x, gy - 120, 220, 120); g.fillStyle = '#5a6068'; g.beginPath(); g.moveTo(x - 10, gy - 118); g.lineTo(x + 110, gy - 160); g.lineTo(x + 230, gy - 118); g.closePath(); g.fill(); g.fillStyle = '#2c3038'; g.fillRect(x + 70, gy - 96, 80, 96); g.fillStyle = 'rgba(255,220,160,.25)'; g.fillRect(x + 76, gy - 90, 68, 84); g.fillStyle = '#243447'; rr(g, x + 20, gy - 140, 180, 22, 4); g.fill(); g.fillStyle = '#f6ecd8'; g.font = 'bold 10px system-ui'; g.textAlign = 'center'; g.fillText('FLEET MAINTENANCE', x + 110, gy - 125); g.fillStyle = '#c0392b'; rr(g, x + 170, gy - 40, 30, 40, 3); g.fill(); g.fillStyle = '#e9d27a'; g.fillRect(x + 176, gy - 34, 18, 4); for (let k = 0; k < 3; k++) { g.fillStyle = '#1b1d22'; g.beginPath(); g.arc(x + 20 + k * 22, gy - 8, 10, 0, Math.PI * 2); g.fill(); } g.textAlign = 'left'; }
function drawFoodTruck(g, x, gy) { g.fillStyle = 'rgba(0,0,0,.2)'; g.beginPath(); g.ellipse(x + 70, gy, 90, 6, 0, 0, Math.PI * 2); g.fill(); g.fillStyle = '#e63946'; rr(g, x, gy - 96, 150, 82, 8); g.fill(); g.fillStyle = '#f2b544'; g.fillRect(x, gy - 60, 150, 6); g.fillStyle = '#243447'; rr(g, x + 20, gy - 88, 90, 30, 3); g.fill(); g.fillStyle = '#e9d27a'; g.fillRect(x + 10, gy - 100, 110, 4); g.fillStyle = '#f6ecd8'; g.fillRect(x + 20, gy - 50, 90, 3); g.fillStyle = '#fff'; g.font = 'bold 10px Georgia'; g.textAlign = 'center'; g.fillText('TINA\'S TACOS', x + 65, gy - 66); g.fillStyle = '#1b1d22'; g.beginPath(); g.arc(x + 30, gy - 8, 11, 0, Math.PI * 2); g.arc(x + 120, gy - 8, 11, 0, Math.PI * 2); g.fill(); g.fillStyle = '#c0392b'; g.fillRect(x + 120, gy - 130, 4, 36); g.fillStyle = '#f6ecd8'; rr(g, x + 96, gy - 146, 60, 18, 3); g.fill(); g.fillStyle = '#c0392b'; g.font = 'bold 8px system-ui'; g.fillText('OPEN 11–2', x + 126, gy - 134); g.textAlign = 'left'; }
function drawWaterTower(g, x, gy) { g.fillStyle = '#8a8f98'; for (const dx of [-30, 30]) { g.fillRect(x + dx - 3, gy - 220, 6, 220); } g.fillRect(x - 40, gy - 120, 80, 4); g.fillRect(x - 40, gy - 60, 80, 4); g.fillStyle = '#c9c5bb'; g.beginPath(); g.ellipse(x, gy - 236, 60, 40, 0, 0, Math.PI * 2); g.fill(); g.fillStyle = '#a9a59b'; g.beginPath(); g.ellipse(x, gy - 262, 58, 14, 0, Math.PI, 0); g.fill(); g.fillStyle = '#243447'; g.font = 'bold 12px Georgia'; g.textAlign = 'center'; g.fillText('PHILLIPS', x, gy - 232); g.fillStyle = '#f2b544'; g.fillRect(x - 30, gy - 228, 60, 2); g.textAlign = 'left'; }
function drawDogPark(g, x, gy) { g.fillStyle = '#6f8f4a'; g.fillRect(x, gy, 360, H - gy); g.fillStyle = '#5a6068'; for (let k = 0; k <= 360; k += 30) g.fillRect(x + k, gy - 40, 3, 40); g.fillRect(x, gy - 40, 360, 2); g.fillRect(x, gy - 20, 360, 2); g.fillStyle = '#243447'; rr(g, x + 120, gy - 76, 120, 22, 4); g.fill(); g.fillStyle = '#f6ecd8'; g.font = 'bold 9px system-ui'; g.textAlign = 'center'; g.fillText('DOG PARK  ·  leashes optional', x + 180, gy - 61); g.fillStyle = '#7a5a3a'; g.fillRect(x + 60, gy - 28, 6, 28); g.fillStyle = '#c0392b'; g.beginPath(); g.arc(x + 63, gy - 30, 5, 0, Math.PI * 2); g.fill(); g.fillStyle = '#8a94a0'; g.fillRect(x + 300, gy - 22, 22, 22); g.fillStyle = '#7fd0ff'; g.fillRect(x + 302, gy - 12, 18, 10); g.textAlign = 'left'; }
function drawDumpsters(g, x, gy) { for (let k = 0; k < 2; k++) { g.fillStyle = k ? '#2f7f4f' : '#3c5fa6'; rr(g, x + k * 90, gy - 44, 76, 44, 4); g.fill(); g.fillStyle = 'rgba(0,0,0,.25)'; g.fillRect(x + k * 90, gy - 48, 76, 8); g.fillStyle = '#f6ecd8'; g.font = 'bold 7px system-ui'; g.textAlign = 'center'; g.fillText(k ? 'RECYCLE' : 'TRASH', x + k * 90 + 38, gy - 22); } g.fillStyle = '#c9a56a'; g.fillRect(x + 190, gy - 30, 40, 30); g.fillStyle = '#7a5a3a'; g.fillRect(x + 190, gy - 32, 40, 3); g.textAlign = 'left'; }
function drawCanal(g, x, gy) { g.fillStyle = '#8a7a5a'; g.fillRect(x, gy, 600, H - gy); g.fillStyle = '#31506a'; g.beginPath(); g.moveTo(x + 40, gy + 30); g.lineTo(x + 560, gy + 30); g.lineTo(x + 520, gy + 90); g.lineTo(x + 80, gy + 90); g.closePath(); g.fill(); g.fillStyle = '#6f8f4a'; g.fillRect(x, gy, 600, 22); g.fillStyle = '#243447'; rr(g, x + 40, gy - 76, 170, 22, 4); g.fill(); g.fillStyle = '#f6ecd8'; g.font = 'bold 9px system-ui'; g.textAlign = 'center'; g.fillText('LEHIGH CANAL  ·  TOWPATH', x + 125, gy - 61); g.fillStyle = '#2c3038'; g.fillRect(x + 123, gy - 54, 3, 54); g.fillStyle = '#5a3a24'; g.fillRect(x + 400, gy - 18, 48, 5); g.fillRect(x + 400, gy - 30, 48, 4); g.fillRect(x + 404, gy - 18, 3, 18); g.fillRect(x + 441, gy - 18, 3, 18); g.textAlign = 'left'; }
function drawBackForty(g, x0, id) {
  const gy = WORLD.groundY, x1 = x0 + BACK_W; g.fillStyle = '#4a4c54'; g.fillRect(x0, gy, 1600, H - gy); g.fillStyle = '#6f8f4a'; g.fillRect(x0 + 1600, gy, BACK_W - 1600, H - gy);
  g.fillStyle = '#243447'; rr(g, x0 + 20, gy - 110, 130, 24, 4); g.fill(); g.fillStyle = '#f6ecd8'; g.font = 'bold 10px system-ui'; g.textAlign = 'center'; g.fillText('TRAILER DROP', x0 + 85, gy - 93); g.fillStyle = '#2c3038'; g.fillRect(x0 + 83, gy - 86, 4, 86);
  for (let k = 0; k < 4; k++) drawTrailerParked(g, x0 + 160 + k * 230, gy, 100 + ((hashName(id) + k * 7) % 80));
  if (['easton', 'lansing', 'billings'].includes(id)) drawBoxcar(g, x0 + 1320, gy, '#7a3b2a', 'NS 4471');
  drawDumpsters(g, x0 + 1600, gy); drawShed(g, x0 + 1880, gy); drawFoodTruck(g, x0 + 2160, gy); drawWaterTower(g, x0 + 2420, gy); drawDogPark(g, x0 + 2520, gy);
  if (id === 'easton') { g.fillStyle = '#243447'; rr(g, x1 - 130, gy - 80, 120, 22, 4); g.fill(); g.fillStyle = '#f6ecd8'; g.font = 'bold 9px system-ui'; g.textAlign = 'center'; g.fillText('WALKING TRAIL →', x1 - 70, gy - 65); g.fillStyle = '#2c3038'; g.fillRect(x1 - 72, gy - 58, 3, 58); }
  g.textAlign = 'left';
}
// Easton gets the canal after the trail
WORLD_DEFS.easton.canalX = null;

/* ---------- animated life out back ---------- */
const backLife = { mule: { x: 0, dir: 1 }, deer: { x: 0, t: 0, run: 0 }, parkDogs: [] };
function updateBackLife(dt) {
  const def = WORLD.def; if (!def || !def.backX) return; const bx = def.backX;
  const m = backLife.mule; if (WORLD.id === 'easton') { if (!m.on) { m.on = true; m.x = def.canalX + 200; } m.x += m.dir * 22 * dt; if (m.x > def.canalX + 520) m.dir = -1; if (m.x < def.canalX + 60) m.dir = 1; }
  const d = backLife.deer; if (WORLD.id === 'easton') { d.t += dt; if (!d.on) { d.on = true; d.x = def.backX + BACK_W - 260; } const near = Math.abs(player.x - d.x) < 220; if (near && !d.run) { d.run = 1; d.dir = player.x < d.x ? 1 : -1; } if (d.run) { d.x += d.dir * 380 * dt; if (Math.abs(d.x - (def.backX + BACK_W - 260)) > 900) { d.run = 0; d.x = def.backX + BACK_W - 260; d.hidden = 12; } } if (d.hidden > 0) d.hidden -= dt; }
  if (!backLife.parkDogs.length) for (let i = 0; i < 3; i++) backLife.parkDogs.push({ x: bx + 2560 + i * 100, dir: 1, t: rnd(0, 5), col: ['#5a4a3a', '#e8e0d0', '#8a8a8a'][i], v: rnd(60, 110), run: 0, facing: 1, moving: true, earA: 0 });
  for (const pd of backLife.parkDogs) { pd.t += dt; pd.x += pd.dir * pd.v * dt; if (pd.x > bx + 2860) pd.dir = -1; if (pd.x < bx + 2550) pd.dir = 1; pd.facing = pd.dir; pd.run = (pd.run + dt * 5) % 1; pd.y = WORLD.groundY; }
}
function drawBackLife(night) {
  const def = WORLD.def; if (!def || !def.backX) return; const bx = def.backX;
  for (const pd of backLife.parkDogs) { if (Math.abs(pd.x - camera.x - W / 2) > W) continue; drawBiscuit(ctx, pd, nearestLight(pd.x, pd.y - 14, night)); }
  if (WORLD.id === 'easton') {
    const m = backLife.mule; if (m.on && Math.abs(m.x - camera.x - W / 2) < W) { drawHorse(ctx, m.x, WORLD.groundY, gameTime); ctx.save(); ctx.translate(m.x, WORLD.groundY); ctx.fillStyle = '#5a3a24'; ctx.fillRect(-32, -60, 6, 20); ctx.fillRect(28, -60, 6, 20); ctx.strokeStyle = '#7a5a3a'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(30, -50); ctx.lineTo(30 + m.dir * -120, -10); ctx.stroke(); ctx.restore(); }
    const d = backLife.deer; if (d.on && !(d.hidden > 0) && Math.abs(d.x - camera.x - W / 2) < W) { ctx.save(); ctx.translate(d.x, WORLD.groundY); ctx.scale(d.run ? d.dir : -1, 1); ctx.fillStyle = '#a8763a'; rr(ctx, -22, -46, 44, 22, 10); ctx.fill(); for (const lx of [-16, -6, 6, 16]) ctx.fillRect(lx, -26, 4, 26 + (d.run ? Math.sin(d.t * 20 + lx) * 4 : 0)); ctx.save(); ctx.translate(22, -46); ctx.rotate(-0.6); rr(ctx, -5, -22, 10, 26, 4); ctx.fill(); ctx.restore(); ctx.fillStyle = '#5a3a24'; ctx.fillRect(28, -70, 2, 10); ctx.fillRect(34, -70, 2, 10); ctx.fillStyle = '#fff'; ctx.fillRect(-24, -40, 5, 5); ctx.restore(); }
  }
}

/* ---------- NPCs out back ---------- */
const BACK_NPCS = [
  ['Lou', 'Fleet Mechanic', { skin: '#c48a62', hair: '#3a2c22', style: 'cap', shirt: '#4a5060', pants: '#2b2f3a', acc: 'none' }, 1990, ['Lou: "Forty trucks, one wrench. The wrench is fine."', 'Lou: "Trailer 106 has a personality. I don\'t mean that as a compliment."']],
  ['Tina', 'Tina\'s Tacos', { skin: '#d9a77c', hair: '#1a1410', style: 'bun', shirt: '#e63946', pants: '#2b2f3a', acc: 'none' }, 2240, ['Tina: "Eleven to two. The robots don\'t eat, so business is steady."', 'Tina: "The gray guy comes by. Never orders. Always tips."']],
  ['Marisol', 'Yard Jockey', { skin: '#c9906a', hair: '#3a2c22', style: 'ponytail', shirt: '#f2b544', pants: '#2b2f3a', acc: 'vest' }, 400, ['Marisol: "I move trailers to doors all night. Tonight the doors moved."', 'Marisol: "Drop yard\'s full. Something keeps bringing them back."']],
  ['Ben', 'Landscaping', { skin: '#f1c9a5', hair: '#a8632c', style: 'short', shirt: '#2f7f4f', pants: '#2b2f3a', acc: 'none' }, 2700, ['Ben: "Dog park\'s mine. The koi are mine. The groundhog is not mine."', 'Ben: "Sprinklers at six. Set your watch."']],
  ['Officer Dee', 'Security', { skin: '#8a5a3a', hair: '#1a1410', style: 'cap', shirt: '#243447', pants: '#1b1d22', acc: 'lanyard' }, 1560, ['Dee: "Badge, please. Kidding. I know who you are. Everybody knows who you are tonight."', 'Dee: "The RV in the lot has been there since Tuesday. I\'ve named it."']],
  ['Priya', 'Runner (lunch)', { skin: '#c9906a', hair: '#1a1410', style: 'ponytail', shirt: '#7fd0ff', pants: '#2b2f3a', acc: 'none' }, 2900, ['Priya: "Two laps of the lot is a mile. Three if the deer\'s out."']],
];
function addBackNpcs() { const def = WORLD.def; if (!def || !def.backX || demo.active) return; for (const [n, role, look, off, lines] of BACK_NPCS) { if (n === 'Tina' && (hour < 10.5 || hour > 14.5) && Math.random() < 0.5) continue; addNPC({ look: Object.assign({ name: n, role }, look), x: def.backX + off, y: groundYAt(def.backX + off), facing: -1, lines }); } if (WORLD.id === 'easton') addNPC({ look: { name: 'Walt', role: 'Canal Mule Driver', skin: '#eec2a0', hair: '#8e8e8e', style: 'cap', shirt: '#7a5a3a', pants: '#3a2a1a', acc: 'none' }, x: def.canalX + 300, y: 440, facing: -1, lines: ['Walt: "Mules walked this towpath before the trucks. The trucks are faster. The mule has never once been locked out of anything."'] }); }

/* ---------- apply ---------- */
for (const id of ['easton', 'taunton', 'spartanburg', 'plantcity', 'lansing', 'billings', 'portland', 'sacramento', 'aurora']) {
  const def = WORLD_DEFS[id]; if (!def || def.backX) continue; const backX = def.width; def.backX = backX; const extra = BACK_W + (id === 'easton' ? 600 : 0); def.width = backX + extra; if (id === 'easton') def.canalX = backX + BACK_W;
  const solids = def.solids; def.solids = () => solids().concat([[backX, 440, extra, 200, 0]]);
  const lights = def.lights; def.lights = () => lights().concat([{ x: backX + 2220, y: 300, r: 220, color: [255, 200, 160], night: 1, cone: 1 }, { x: backX + 1960, y: 320, r: 200, color: [255, 235, 190], night: 1, cone: 1 }, { x: backX + 2700, y: 300, r: 220, color: [255, 235, 190], night: 1, cone: 1 }, { x: backX + 700, y: 290, r: 260, color: [255, 235, 190], night: 1, cone: 1 }]);
  const play = def.play; def.play = (g) => { play(g); drawBackForty(g, backX, id); if (id === 'easton') { drawCanal(g, def.canalX, WORLD.groundY); drawOak(g, def.canalX + 20, WORLD.groundY, 0.9); drawOak(g, def.canalX + 585, WORLD.groundY, 1.0); } };
  def.breakpoints = (def.breakpoints || []).concat([backX + 1200, backX + 2100, backX + 2800]);
  if (id === 'easton') def.water = (def.water || []).concat([{ x0: def.canalX + 40, x1: def.canalX + 560, y: 470 }]);
}
const _updateAplus10 = updateAplus; updateAplus = function (dt) { _updateAplus10(dt); updateBackLife(dt); };
const _drawStoryLayer8 = drawStoryLayer; drawStoryLayer = function (night) { _drawStoryLayer8(night); drawBackLife(night); };
const _loadWorld18 = loadWorld; loadWorld = function (id, at) { _loadWorld18(id, at); backLife.mule.on = false; backLife.deer.on = false; backLife.parkDogs.length = 0; addBackNpcs(); };
