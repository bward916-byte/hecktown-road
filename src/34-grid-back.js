/* the coverflow was too subtle on a phone — back to the row of cards */
(function () { const st = document.createElement('style'); st.textContent = '#flow, #flowArrows, #carousel, #dots { display:none !important; } #grid { display:flex !important; }'; document.head && document.head.appendChild(st); window.removeEventListener && 0; })();
